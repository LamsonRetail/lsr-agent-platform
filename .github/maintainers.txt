# Maintainers được phép sửa CORE (mỗi dòng 1 GitHub username, phân biệt hoa/thường).
# Scope-guard cho phép các user này chạm core; người khác chỉ được sửa agent của mình.
# → THÊM username GitHub thật của admin/core team vào đây (bỏ dòng ví dụ).
# vi-du-username-admin
