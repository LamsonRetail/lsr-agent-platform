# Copy thành .env rồi điền (KHÔNG commit .env — đã gitignore ở gốc repo)
LSR_AGENT_TOKEN=lsr_tel_...
DRY_RUN=true

# Chỉ cần khi chạy ingest/bigquery_sync.py thật (đọc-chỉ):
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# BIGQUERY_PROJECT=...
